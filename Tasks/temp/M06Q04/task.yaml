accessible: true
author: Doriane Olewicki et Marie-Marie van der Beek
context: |-
    Ecrivez une fonction qui compare l'objet cours avec un autre donné en argument.


    .. code-block:: java

        class Cours {
            private String prof;
            private String code;
            private int credit;

            /*
             * @pre  -
             * @post crée un objet Cours dont le prof est pr, le code est co et le nombre de
             * crédit cr.
             */
            public Cours (String pr, String co, int cr) { /* non fourni */ }

            /*
             * @pre  -
             * @post crée un objet Cours dont le prof, le code et le nombre de
             * crédit sont donnés par texte.
             */
            public Cours (String texte) { /* non fourni */ }

            /*
             * @pre -
             * @post renvoie, respectivement, le nom du prof, le code du cours
             * et le nombre de crédit
             */
            public String getProf() { /* non fourni */ }
            public String getCode() { /* non fourni */ }
            public int getCredit() { /* non fourni */ }

            /*
             * @pre -
             * @post mets à jour, respectivement, le nom du prof, le code du cours
             * et le nombre de crédit
             */
            public void setProf(String pr) { /* non fourni */ }
            public void setCode(String co) { /* non fourni */ }
            public void setCredit(int cr) { /* non fourni */ }

            /* A COMPLETER */
        }
environment: java7
groups: false
limits:
    memory: '100'
    time: '30'
    output: '2'
name: 'Mission 6 : Question de démarrage - classe Cours 2.2 (le comparateur)'
network_grading: false
problems:
    q1:
        language: java
        type: code
        name: Comparaison de cours
        header: |-
            Fournissez le code répondant à la signature suivante :

            .. code-block:: java

                /*
                 * @pre  -
                 * @post renvoie true si les objets sont identiques, false sinon.
                 * Si l'objet comparé est null, renvoie false.
                 */
                public boolean equals (Cours cours) {
                    /* A COMPLETER */
                }
weight: 1.0
